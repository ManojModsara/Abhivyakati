@model List<QuizGame.Dto.EventDto>

@foreach (var item in Model)
{
    <div class="item">
        <a href="~/Site/EventDetails/@item.Title"
           class="group bg-white rounded-3xl shadow-lg hover:shadow-2xl transition-all duration-500 overflow-hidden transform hover:-translate-y-2 block">
            <div class="relative overflow-hidden">
                <img src="@item.ShowImg" alt="@item.Title"
                     class="w-full h-48 object-cover transform group-hover:scale-110 transition-transform duration-700" />
                <div class="absolute top-4 left-4 bg-red-500 text-white px-3 py-1 rounded-full text-xs font-bold">
                    <i class="fas fa-calendar mr-1"></i>@item.EvtDate
                </div>
            </div>
            <div class="p-6">
                <h4 class="text-lg font-bold text-gray-900 group-hover:text-purple-600 transition-colors mb-2">
                    @item.Title
                </h4>
                @*<p class="text-gray-600 text-sm mb-4">
                    @Html.Raw(item.Description)
                </p>*@
                <p>Read More ...</p>

                @*<div class="flex items-center gap-2 text-gray-500 text-xs">
            <i class="fas fa-map-marker-alt text-purple-500"></i>
            <span>Community Center, Jaipur</span>
        </div>*@
            </div>
        </a>
    </div>
}


